using UnityEngine;

public static class Constants
{
    public enum ESceneType
    {
        // Main,
        // Game,
        // Ending

        PrototypeMain,
        PrototypeGame,
        Ending
    }
    public enum ESaveLoadType
    {
        Save,
        Load
    }
}