using UnityEngine;

public static class Constants
{
    public enum ESceneType
    {
        /*
        Main,
        Game,
        Ending
        */
        TestMainScene,
        TestScene
    }
    public enum ESaveLoadType
    {
        Save,
        Load
    }
}