using UnityEngine;

public static class Constants
{
    public enum ESceneType
    {
        // Main,
        // Game,
        // Ending

        TestMainScene,
        TestScene,
        Ending
    }
    public enum ESaveLoadType
    {
        Save,
        Load
    }
}